//* Атрибути в якості властивостей об'єкту

// const imgEl = document.querySelector('.js-img');

// console.dir(imgEl);

// imgEl.src = 'https://picsum.photos/200/300';
// imgEl.alt = 'Random image';

// https://picsum.photos/200/300

/*
 * Доступ до атрибутів
 * elem.hasAttribute(name)
 * elem.getAttribute(name)
 * elem.setAttribute(name, value)
 * elem.removeAttribute(name)
 * elem.attributes
 */
// const imgEl = document.querySelector('.js-img');

// console.log(imgEl.hasAttribute('id'));

// console.log(imgEl.getAttribute('src'));
// console.log(imgEl.src);

// imgEl.setAttribute('id', 'my image');
// imgEl.id = 'my image';

// imgEl.removeAttribute('id');

// console.log(imgEl.attributes);
